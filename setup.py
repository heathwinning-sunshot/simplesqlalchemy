import setuptools

setuptools.setup(
    name="simplesqlalchemy",
    version="0.0.1",
    author="Heath Winning",
    description="A small wrapper around sqlalchemy for to reduce boilerplate with pandas",
    packages=setuptools.find_packages(),
    python_requires=">=3.8",
)